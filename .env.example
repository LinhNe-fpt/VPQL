# SQL Server – kết nối SSMS / ứng dụng Stockflow
# Sao chép thành .env và chỉnh server nếu dùng SQLEXPRESS / LocalDB

# Web app – mạng nội bộ (truy cập: http://<IP-máy-chủ>:3636)
PORT=3636
HOST=0.0.0.0

DB_SERVER=localhost
DB_PORT=1433
DB_NAME=VPP
DB_USER=ysv
DB_PASSWORD=123
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true

# Ví dụ SQL Server Express trên Windows:
# DB_SERVER=localhost\\SQLEXPRESS
